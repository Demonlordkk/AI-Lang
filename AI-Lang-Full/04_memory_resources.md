# AI-Lang Phase 4 — Memory & Resource Model v0.1
The reference runtime manages language values through its host memory system. External resources are explicit and failures are reported rather than silently ignored. Future resource constructs may provide deterministic cleanup.
