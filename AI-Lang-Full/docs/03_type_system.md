# AI-Lang Phase 3 — Type System v0.1
Core types: Int, Float, Bool, Text, List, Map, and Null. The reference runtime carries explicit type information and rejects invalid operations. Numeric conversion is explicit in the core.
