[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-lang-toolchain"
version = "1.0.0"
description = "AI-Lang reference compiler, bytecode VM, and developer toolchain"
requires-python = ">=3.10"

[project.scripts]
ailang = "22_cli:main"
